import { getServerSession } from 'next-auth';
import { authOptions } from '@/lib/auth';
import { prisma } from '@/lib/prisma';
import { NextResponse } from 'next/server';
export async function POST(req:Request){const s=await getServerSession(authOptions);if(!s)return NextResponse.json({error:'Unauthorized'},{status:401});const d=await req.json();const slug=d.slug||d.name.toLowerCase().replace(/[^a-z0-9]+/g,'-');return NextResponse.json(await prisma.product.create({data:{name:d.name,slug,price:Number(d.price),description:d.description,colorId:d.colorId,stock:Number(d.stock||0),imagePath:d.imagePath||null}}))}
export async function PUT(req:Request){const s=await getServerSession(authOptions);if(!s)return NextResponse.json({error:'Unauthorized'},{status:401});const d=await req.json();return NextResponse.json(await prisma.product.update({where:{id:d.id},data:{name:d.name,slug:d.slug,price:Number(d.price),description:d.description,colorId:d.colorId,imagePath:d.imagePath||null}}))}
export async function DELETE(req:Request){const s=await getServerSession(authOptions);if(!s)return NextResponse.json({error:'Unauthorized'},{status:401});const {id}=await req.json();try{await prisma.product.delete({where:{id}})}catch(e:any){if(e?.code==='P2003')return NextResponse.json({error:'Produk tidak bisa dihapus karena masih punya riwayat order atau stok. Hapus/ubah data terkait dulu.'},{status:409});return NextResponse.json({error:'Gagal menghapus produk.'},{status:500})}return NextResponse.json({ok:true})}
