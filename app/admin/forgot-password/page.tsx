'use client';
import { useState } from 'react';
export const dynamic = 'force-dynamic';
export default function ForgotPassword(){const [username,setUsername]=useState(''),[sent,setSent]=useState(false),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);await fetch('/api/admin/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username})});setLoading(false);setSent(true);}return <main className="hero"><form className="checkout-card" onSubmit={submit}><div className="eyebrow">LUMIÉRA Shine</div><h2>Lupa Password</h2>{sent?<p>Kalau username terdaftar dan punya email, link reset sudah dikirim. Cek inbox kamu.</p>:<><p>Masukkan username admin, kami akan kirim link reset ke email yang terdaftar.</p><input className="form-control" aria-label="Username" placeholder="Username" value={username} onChange={e=>setUsername(e.target.value)}/><button className="button" disabled={loading}>{loading?'Mengirim...':'Kirim Link Reset'}</button></>}<p><a href="/admin/login">Kembali ke login</a></p></form></main>}
