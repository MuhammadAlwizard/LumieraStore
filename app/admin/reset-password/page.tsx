'use client';
import { useSearchParams } from 'next/navigation';
import { Suspense, useState } from 'react';
export const dynamic = 'force-dynamic';
function ResetForm(){const token=useSearchParams().get('token')||'';const [password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[show,setShow]=useState(false),[error,setError]=useState(''),[done,setDone]=useState(false),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError('');if(password!==confirm){setError('Konfirmasi password tidak cocok.');return;}setLoading(true);const r=await fetch('/api/admin/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});const data=await r.json();setLoading(false);if(!r.ok){setError(data.error||'Terjadi kesalahan.');return;}setDone(true);setTimeout(()=>{location.href='/admin/login';},2000);}if(!token)return <p>Link reset tidak valid. <a href="/admin/forgot-password">Minta link baru</a>.</p>;if(done)return <p>Password berhasil diganti. Mengalihkan ke halaman login...</p>;return <form onSubmit={submit}><h2>Buat Password Baru</h2>{error&&<p className="error">{error}</p>}<div style={{position:'relative'}}><input className="form-control" style={{paddingRight:48}} aria-label="Password baru" type={show?'text':'password'} placeholder="Password baru" value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" aria-label={show?'Sembunyikan password':'Tampilkan password'} onClick={()=>setShow(!show)} style={{position:'absolute',right:10,top:10,border:0,background:'transparent',cursor:'pointer'}}><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7S2 12 2 12Z"/><circle cx="12" cy="12" r="3"/></svg></button></div><input className="form-control" aria-label="Konfirmasi password baru" type={show?'text':'password'} placeholder="Konfirmasi password baru" value={confirm} onChange={e=>setConfirm(e.target.value)}/><button className="button" disabled={loading}>{loading?'Menyimpan...':'Simpan Password'}</button></form>}
export default function ResetPassword(){return <main className="hero"><div className="checkout-card"><div className="eyebrow">LUMIÉRA Shine</div><Suspense fallback={<p>Memuat...</p>}><ResetForm/></Suspense><p><a href="/admin/login">Kembali ke login</a></p></div></main>}
