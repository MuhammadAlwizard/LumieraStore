import { prisma } from '@/lib/prisma';
import Link from 'next/link';
import AnalyticsSection from '@/components/AnalyticsSection';
export const dynamic='force-dynamic';
export default async function AdminPage(){const [products,orders]=await Promise.all([prisma.product.findMany({include:{color:true},orderBy:{createdAt:'asc'}}),prisma.order.findMany({orderBy:{createdAt:'desc'}})]);return <main className="admin-body"><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:12,flexWrap:'wrap'}}><h1>Dashboard</h1><div><Link className="button" href="/admin/warna">Kelola Warna</Link> <Link className="button" href="/admin/produk/baru">+ Produk</Link></div></div><p>{products.length} produk · {orders.length} order</p><h2>Produk</h2><table className="admin-table"><thead><tr><th>Nama</th><th>Warna</th><th>Stok</th><th>Harga</th><th>Aksi</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td>{p.name}</td><td><span style={{display:'inline-block',width:14,height:14,background:p.color.hex,verticalAlign:'middle',marginRight:5}}/>{p.color.name}</td><td>{p.stock}</td><td>Rp {p.price.toLocaleString('id-ID')}</td><td><Link className="link" href={`/admin/produk/${p.id}`}>Edit</Link> · <Link className="link" href={`/admin/produk/${p.id}/stok`}>Tambah Stok</Link></td></tr>)}</tbody></table><h2 style={{marginTop:40}}>Order Terbaru</h2><table className="admin-table"><thead><tr><th>Order</th><th>Pelanggan</th><th>Total</th><th>Status</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>{o.orderId}</td><td>{o.customerName}<br/>{o.customerEmail}</td><td>Rp {o.total.toLocaleString('id-ID')}</td><td>{o.status}</td></tr>)}</tbody></table><AnalyticsSection/></main>}
