import Link from 'next/link';
type Product={id:string;name:string;slug:string;price:number;description:string;color:string;imagePath:string|null};
export default function ProductCard({product}:{product:Product}){const cls=product.color==='Burgundy'?'color-burgundy':product.color==='Oatmeal'?'color-oatmeal':product.color==='Stone Grey'?'color-stone':'color-sage';return <article className="product-card"><div className={`product-visual ${cls}`}>{product.imagePath?<img src={product.imagePath} alt={product.name} style={{width:'100%',height:'100%',objectFit:'cover'}}/>:product.name}</div><div className="product-body"><h3>{product.name.replace(` ${product.color}`,'')}</h3><div className="product-color">{product.color}</div><p>{product.description}</p><div className="price">Rp {product.price.toLocaleString('id-ID')}</div><Link className="link" href={`/produk/${product.slug}`}>Lihat detail →</Link></div></article>}
