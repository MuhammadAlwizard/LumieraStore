import Link from 'next/link';
type Product={id:string;name:string;slug:string;price:number;description:string;color:{name:string;hex:string};stock:number;imagePath:string|null};
export default function ProductCard({product}:{product:Product}){const unavailable=product.stock<=0;return <article className="product-card"><div className="product-visual" style={{background:product.color.hex}}>{product.imagePath?<img src={product.imagePath} alt={product.name} style={{width:'100%',height:'100%',objectFit:'cover'}}/>:product.name}</div><div className="product-body"><h3>{product.name.replace(` ${product.color.name}`,'')}</h3><div className="product-color">{product.color.name}</div><p>{product.description}</p><div className="price">Rp {product.price.toLocaleString('id-ID')}</div>{unavailable?<span className="error">Habis</span>:<Link className="link" href={`/produk/${product.slug}`}>Lihat detail →</Link>}</div></article>}
