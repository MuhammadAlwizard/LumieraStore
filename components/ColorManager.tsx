'use client';
import { useEffect, useState } from 'react';
type Color={id:string;name:string;hex:string};
export default function ColorManager(){const [colors,setColors]=useState<Color[]>([]);const [name,setName]=useState(''),[hex,setHex]=useState('#8B3A3A'),[message,setMessage]=useState('');async function load(){setColors(await (await fetch('/api/admin/colors')).json())}useEffect(()=>{load()},[]);async function add(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/colors',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,hex})});const j=await r.json();setMessage(r.ok?'Warna ditambahkan.':j.error);if(r.ok){setName('');await load()}}async function remove(id:string){const r=await fetch('/api/admin/colors',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});const j=await r.json();setMessage(r.ok?'Warna dihapus.':j.error);if(r.ok)load()}async function edit(c:Color){const nextName=prompt('Nama warna',c.name);const nextHex=prompt('HEX warna',c.hex);if(!nextName||!nextHex)return;const r=await fetch('/api/admin/colors',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:c.id,name:nextName,hex:nextHex})});const j=await r.json();setMessage(r.ok?'Warna diperbarui.':j.error);if(r.ok)load()}return <><form className="admin-card" onSubmit={add}><input className="form-control" required placeholder="Nama warna" value={name} onChange={e=>setName(e.target.value)}/><div style={{display:'flex',gap:8,alignItems:'center'}}><input type="color" value={hex} onChange={e=>setHex(e.target.value)}/><input className="form-control" required pattern="^#[0-9A-Fa-f]{6}$" value={hex} onChange={e=>setHex(e.target.value.toUpperCase())}/></div><button className="button">Tambah Warna</button>{message&&<p className="success">{message}</p>}</form><table className="admin-table"><thead><tr><th>Warna</th><th>Nama</th><th>HEX</th><th>Aksi</th></tr></thead><tbody>{colors.map(c=><tr key={c.id}><td><span style={{display:'inline-block',width:22,height:22,background:c.hex,border:'1px solid #ccc'}}/></td><td>{c.name}</td><td>{c.hex}</td><td><button className="link" onClick={()=>edit(c)}>Edit</button> <button className="link" onClick={()=>remove(c.id)}>Hapus</button></td></tr>)}</tbody></table></>}
