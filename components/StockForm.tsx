'use client';
import { useState } from 'react';
export default function StockForm({productId}:{productId:string}){const [quantity,setQuantity]=useState(''),[note,setNote]=useState(''),[message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/stock',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({productId,quantity,note})});const j=await r.json();setMessage(r.ok?'Stok berhasil ditambahkan.':j.error);if(r.ok){setQuantity('');setNote('');}}return <form onSubmit={submit}><input className="form-control" type="number" min="1" required placeholder="Jumlah stok" value={quantity} onChange={e=>setQuantity(e.target.value)}/><input className="form-control" placeholder="Catatan (opsional)" value={note} onChange={e=>setNote(e.target.value)}/><button className="button">Tambah Stok</button>{message&&<p className="success">{message}</p>}</form>}
